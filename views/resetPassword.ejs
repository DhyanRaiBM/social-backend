<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>

    <style>
      body {
        font-family: Arial, sans-serif;
        text-align: center;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background-color: rgba(0, 0, 0, 0.2);
      }
      .card {
        border-radius: 10px;
        padding: 20px;
        background-color: white;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
      }
      .icon {
        font-size: 64px;
        margin-bottom: 20px;
      }
      .success {
        color: green;
      }
      .error {
        color: red;
      }
      .hideBtn {
        display: none;
      }
      .showBtn {
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 5px;
        padding: 10px;
        background-color: rgba(1, 116, 210, 0.971);
        color: white;
        margin-top: 10px;
        cursor: pointer;
        transition: background-color 0.3s ease;
        text-decoration: none;
      }
      .showBtn:hover {
        background-color: #2980b9;
      }
      form {
        max-width: 400px;
        padding: 30px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      h2 {
        text-align: center;
        color: #333;
      }

      .form-group {
        position: relative;
        margin-bottom: 10px;
        margin-right: 30px;
      }

      label {
        display: block;
        font-weight: bold;
        margin-bottom: 5px;
        color: #555;
      }

      input[type="text"] {
        width: 100%;
        padding: 15px;
        border: none;
        border-radius: 10px;
        font-size: 18px;
        background: #f9f9f9;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        outline: none;
        transition: background-color 0.3s, box-shadow 0.3s;
      }

      input[type="text"]:focus {
        background-color: #fff;
        box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
      }
    </style>
  </head>
  <body>
    <div class="card">
      <% if (!obj.id) { %>
      <div class="icon">
        <span id="statusIcon"><%= obj.icon %></span>
      </div>
      <div id="statusMessage" class="<%=obj.class %>"><%= obj.message %></div>
    </div>
    <% } else { %>
    <form action="/users/reset-password" method="get">
      <div class="form-group">
        <label for="password">New Password:</label>
        <input
          type="text"
          id="password"
          name="password"
          placeholder="Enter your password"
          autocomplete="off"
          required
        />
        <input
          type="text"
          name="userId"
          value="<%= obj.id %>"
          style="display: none"
        />
      </div>
      <button
        type="submit"
        id="btn"
        class="showBtn"
        style="margin-left: 70px; outline: none; border: none"
      >
        Change Password
      </button>
    </form>
    <% } %>
  </body>
</html>
